import { Observable } from "rxjs";

/**
 * Create observables for each side effect
 */
const taskStarts = new Observable();
const taskCompletions = new Observable();
const showLoader = new Observable();

export default {};
